# MQTT Prototype

Owner: Backend.

Status: prototype MQTT experiments, not production broker infrastructure.

Moved here from the older `src/prototypes/api/mqtt` path during the prototype
folder cleanup.

The production MQTT broker configuration remains `src/Components/MQTT-Server`.
